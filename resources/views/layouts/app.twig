<!doctype html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <title>{% block title %}{% endblock %}</title>
    <style>
       h1, h2, h3, p { font-family: 'Roboto', sans-serif }
        body {padding-top: 24px;}
    </style>
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-md-3 col-sm-12">
            <div class="list-group">
                {% if user.id is empty %}
                    <a href="" class="list-group-item list-group-item-action active">Главная страница</a>
                {% else %}
                    <a href="{{ route('home', {'accessToken': accessToken}) }}" class="list-group-item list-group-item-action">Главная страница</a>
                {% endif %}

                {% for item in users %}
                    {% if item.id == user.id %}
                        <a href="#" class="list-group-item list-group-item-action active">{{ item.name }}</a>
                    {% else %}
                        <a href="{{ route('view', {'accessToken': accessToken, 'userId': item.id}) }}" class="list-group-item list-group-item-action">{{ item.name }}</a>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
        <div class="col-md-9 col-sm-12">
            {% block content %}{% endblock %}
        </div>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
</body>
</html>